package Pages;

import org.apache.log4j.Logger;
import org.openqa.selenium.WebDriver;

public class LoginPageHelper extends LoginPage{
    public LoginPageHelper(WebDriver driver, Logger log) {
        super(driver, log);
    }

}
